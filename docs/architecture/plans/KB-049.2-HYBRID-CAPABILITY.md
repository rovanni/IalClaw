# PLANO DE CORRECAO - KB-049.2

Data: 7 de abril de 2026
Status: Em execucao
Risco: Medio
Escopo: reconciliacao hibrida de capabilities com runtime como fonte de verdade

---

## CONTEXTO

O projeto IalClaw esta em evolucao para o modelo Single Brain, no qual o `CognitiveOrchestrator` e o unico decisor.

Estado validado antes desta etapa:

- o `CapabilityResolver` continua existindo como hipotese semantica
- o mapper `CAPABILITY_TO_SKILLS` ja existe e deve ser mantido apenas como fallback
- o `SkillManager` ja expõe `listSkillIds()` e registra skills builtin por runtime
- o carregamento de skills externas ja le `skill.json.capabilities`, mas isso ainda nao participa como fonte de verdade na reconciliacao do Orchestrator

Definicao de pronto desta etapa:

- skills carregadas em runtime passam a compor indice dinamico por capability
- o `CognitiveOrchestrator` reconcilia runtime + mapper sem deslocar autoridade decisoria
- o mapper continua apenas como fallback sem virar fonte primaria
- logs estruturados registram a reconciliacao por capability
- compilacao TypeScript passa sem regressao atribuivel a esta etapa

---

## REGRA CRITICA - VERIFICAR ANTES DE IMPLEMENTAR

Antes de alterar codigo:

- verificar se ja existe metodo equivalente a `getCapabilityIndex()`
- verificar contrato real da skill em runtime
- reutilizar `hasSkill()` e `listSkillIds()` se ja existirem
- evitar duplicacao entre `SkillManager`, mapper e `CognitiveOrchestrator`

Proibido nesta etapa:

- mover decisao para fora do `CognitiveOrchestrator`
- transformar `SkillManager` em mini-brain
- remover o Final Gate
- substituir o `CapabilityResolver` por heuristica nova

---

## ESTRATEGIA INCREMENTAL OBRIGATORIA

Executar exatamente nesta ordem:

1. criar metodo vazio em `SkillManager`
2. compilar
3. implementar logica minima do indice dinamico
4. compilar
5. integrar reconciliacao hibrida no `CognitiveOrchestrator`
6. compilar

Regra operacional:

- nao implementar tudo de uma vez
- se surgir problema fora do escopo, apenas reportar

---

## FASE TECNICA

### PASSO 1 - Contrato das skills

Objetivo:

- garantir compatibilidade retroativa com `capabilities?: string[]` sem quebrar o contrato atual baseado em `provides`

Checklist:

- [ ] contrato revisado sem duplicar fonte de verdade
- [ ] compatibilidade retroativa preservada

### PASSO 2 - Indice dinamico no SkillManager

Objetivo:

- expor `getCapabilityIndex(): Record<string, string[]>` baseado nas skills carregadas em runtime

Checklist:

- [ ] Index dinâmico de capabilities criado
- [ ] metodo nao altera comportamento existente

### PASSO 3 - Integracao no Orchestrator

Objetivo:

- reconciliar runtime e mapper sem mover autoridade para fora do `CognitiveOrchestrator`

Checklist:

- [ ] Integração no Orchestrator
- [ ] Mapper mantido como fallback
- [ ] Sem regressão de comportamento

### PASSO 4 - Observabilidade

Objetivo:

- registrar reconciliacao hibrida por capability

Checklist:

- [ ] Logs estruturados adicionados

---

## VALIDACAO OBRIGATORIA

Validar ao final:

1. inconsistencias
2. duplicacoes
3. riscos arquiteturais
4. coerencia de autoridade
5. governanca do Final Gate e `input_gap`
6. validacao estrutural de escopo e classe
7. compilacao com `npx tsc --noEmit`

---

## CHECKLIST MINIMO

- [ ] Index dinâmico de capabilities criado
- [ ] Integração no Orchestrator
- [ ] Mapper mantido como fallback
- [ ] Logs estruturados adicionados
- [ ] Sem regressão de comportamento